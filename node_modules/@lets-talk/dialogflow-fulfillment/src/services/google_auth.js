const { JWT } = require('google-auth-library');


function getJWTClient(params) {
    return new JWT(
      params.df_client_email,
      null,
      params.df_private_key,
      ['https://www.googleapis.com/auth/cloud-platform'],
    );
  }

  exports.getJWTClient = getJWTClient;